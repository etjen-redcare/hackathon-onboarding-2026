# Redcare onboarding prototype

Static English prototype for the proposed Redcare iOS onboarding flow.

Publish this folder with GitHub Pages from the repository root or from a `gh-pages` branch. The entry point is `index.html`.
